# Add these to the server environment. Never commit live values.
GEMINI_API_KEY=
GEMINI_TEXT_MODEL=gemini-2.5-flash
ADMIN_PASSWORD=
# Set a stable random secret in production, at least 32 characters.
APP_SECRET=
HOST=127.0.0.1
PORT=3000
DATA_DIR=./data
